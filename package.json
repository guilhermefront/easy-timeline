{
  "private": true,
  "scripts": {
    "dev": "concurrently 'cd ./frontend && yarn dev -p 3001' 'cd ./backend && yarn dev'",
    "dev:fe": "cd ./frontend && yarn dev",
    "dev:be": "cd ./backend && yarn dev"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
